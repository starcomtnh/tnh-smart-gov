### Login to the system
POST {{wso2_issue_url}} HTTP/1.1
Content-Type: application/x-www-form-urlencoded
Authorization: {{wso2_credentials}}

grant_type = password &
username = admin &
password = admin &
scope = openid

### Synchronize privileges
POST {{base_url}}/privileges/sync HTTP/1.1
Authorization: {{access_token}}

### Logout from the system
POST {{base_url}}/auth/logout HTTP/1.1
Authorization: Bearer {{access_token}}

### Refresh the access token
POST {{base_url}}/auth/refresh-token HTTP/1.1
Authorization: Bearer {{refresh_token}}
Content-Type: application/json

{
  "access_token": "{{access_token}}",
  "refresh_token": "{{refresh_token}}"
}

### Register a new user
POST {{base_url}}/auth/register HTTP/1.1
Content-Type: application/json

{
  "username": "string",
  "password": "string",
  "first_name": "string",
  "last_name": "string",
  "full_name": "string",
  "phone": "string",
  "email": "string",
  "avatar_url": "string",
  "idn": "string",
  "ial": 0
}